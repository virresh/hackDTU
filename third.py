# -*- coding: utf-8 -*-
from Tkinter import *
root = Tk(className ="Addiction") #add a root window named Myfirst GUI
foo = Label(root,text='''Alcohol, Smoking and caffeine create adverse effects in the body. Only one solution - If you can’t stop,
reduce the intake. Try organic food matters as a supplement.
''') # add a label to root window
foo.pack()
root.mainloop()
